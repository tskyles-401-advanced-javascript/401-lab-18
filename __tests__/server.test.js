'use strict';

describe('app', () => {
  it('should pass some sort of test', () => {
    const test = 'pass';
    expect(test).toBe('pass');
  });
  xit('logs when students connect to room', () => {

  });
  xit('logs when teacher connects to room', () => {

  });
});